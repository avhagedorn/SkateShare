Low:

  - Notify user of profile file size / upload success
  - Fix user creation hashmap placement. Maybe create utility function(s)?
  - Add "like" functionality to posts

Medium:

  - Fix back button issue after creating post
  - Address time standardization issue with feed
  - RecyclerView state not persisted across bottom navigation actions
  - Add hashmap optimization to user data queries in the feed
